# Railway Environment Variables Template
# Copy this to Railway dashboard → Variables tab

# Server
PORT=3002

# Database — PostgreSQL (REQUIRED for production / Railway)
# Railway auto-sets this when you add a PostgreSQL addon
# Local dev: leave empty to use SQLite
DATABASE_URL=postgresql://user:password@host:5432/ghost_db

# CORS - add your Railway frontend URL after deployment
CORS_ORIGINS=http://localhost:3000,https://your-frontend.vercel.app

# Aleo Network (optional - for indexer service)
ALEO_ENDPOINT=https://api.explorer.provable.com/v1
ALEO_NETWORK=testnet
